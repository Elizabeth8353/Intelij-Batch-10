Collections:
The collection Framework is a group of objects represented as a single unit
The collection Framework it defines different interfaces and classes by which we can represent a group of objects into collection
The collection Framework is a collection of classes and interfaces that can hold objects as a single unit like arrays but with a
lot of flexibility.

